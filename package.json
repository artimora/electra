{
	"name": "electra",
	"module": "index.ts",
	"type": "module",
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"workspaces": [
		"src/apps/*",
		"src/packages/*"
	],
	"scripts": {
		"build": "cd src/packages/electra && bun run build",
		"dev": "cd src/packages/electra && bun run dev"
	}
}
